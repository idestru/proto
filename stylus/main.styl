@import 'nib'
@import 'mixins/fonts'

// webfont(family, file, weight = 'normal', hack-chrome-windows = false)
// webfont('super-duper-font', 'file', 'bold', true)

body
	font 1em Arial, sans-serif
	color #333
	background #fff
.container
	padding 20px
	width 60%
	margin 0 auto
	box-shadow: 0 5px 5px rgba(0, 0, 0, .5)